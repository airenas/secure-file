#####################################################################################
tag=$(USER)/secure-demo:0.1
#####################################################################################
dbuild: 
	docker build -t $(tag) --build-arg SECRET=$(secret) ./

dpush: dbuild
	docker push $(tag)
#####################################################################################
.PHONY:
	dbuild dpush
