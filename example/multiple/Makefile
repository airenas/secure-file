#####################################################################################
tag=$(USER)/secure-demo-multiple:0.1
#####################################################################################
dbuild: 
	docker build -t $(tag) --build-arg SECRET=$(secret) ./

dpush: dbuild
	docker push $(tag)
#####################################################################################
.PHONY:
	dbuild dpush
